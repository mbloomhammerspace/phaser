apiVersion: v1
kind: Pod
metadata:
  name: rag-collections-test
  namespace: default
spec:
  restartPolicy: Never
  containers:
  - name: test
    image: busybox
    command: ["sh", "-c", "echo 'Testing rag-server collections...' && wget -qO- http://rag-server:8081/collections && echo 'collections test complete'"]
