apiVersion: v1
kind: Service
metadata:
  name: rag-server-mcp
  namespace: default
  labels:
    app: rag-server-mcp
spec:
  selector:
    app: rag-server-mcp-enhanced
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
  type: ClusterIP
