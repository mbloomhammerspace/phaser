apiVersion: v1
kind: Pod
metadata:
  name: nfs-mount-test-private
spec:
  nodeSelector:
    kubernetes.io/hostname: worker-node-1
  containers:
  - name: nfs-mount-test
    image: busybox
    command: ["sh", "-c", "mkdir -p /mnt/nfs && mount -t nfs 10.0.0.161:/mnt/iscsi /mnt/nfs && df -h /mnt/nfs && ls -la /mnt/nfs && umount /mnt/nfs && echo 'NFS mount test with private IP completed successfully'"]
    securityContext:
      privileged: true
  restartPolicy: Never
